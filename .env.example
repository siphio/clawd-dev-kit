# ═══════════════════════════════════════════════════════════════════════════════
# ClawdDev Kit Configuration
# ═══════════════════════════════════════════════════════════════════════════════
#
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# IMPORTANT: Never commit .env to Git - it contains sensitive information
# ═══════════════════════════════════════════════════════════════════════════════

# ─────────────────────────────────────────────────────────────────────────────────
# MAC MINI SSH CONNECTION
# ─────────────────────────────────────────────────────────────────────────────────
# These settings allow the ClawdDev Kit to deploy capabilities to your production
# Mac Mini without you needing to physically access it.

# Hostname or IP address of your Mac Mini
# Examples: macmini.local, 192.168.1.100, mymini.home
CLAWD_MINI_HOST=your-macmini.local

# Your username on the Mac Mini
CLAWD_MINI_USER=your-username

# Path to SSH private key for Mac Mini authentication
# This key must be added to ~/.ssh/authorized_keys on the Mini
# Generate with: ssh-keygen -t ed25519 -f ~/.ssh/id_ed25519_clawd_mini
CLAWD_MINI_SSH_KEY=~/.ssh/id_ed25519_clawd_mini

# ─────────────────────────────────────────────────────────────────────────────────
# CLAWDBOT WORKSPACE PATHS
# ─────────────────────────────────────────────────────────────────────────────────

# Production workspace on Mac Mini (where Clawdbot runs 24/7)
CLAWD_MINI_WORKSPACE=~/clawd

# Local development workspace (for testing before deployment)
CLAWD_DEV_WORKSPACE=~/clawd-dev

# ─────────────────────────────────────────────────────────────────────────────────
# GIT REPOSITORY (Optional)
# ─────────────────────────────────────────────────────────────────────────────────
# If using Git-based deployment, configure your repository

# Git repository URL for Clawdbot capabilities
# This should be a private repo that both dev machine and Mini can access
CLAWD_CAPABILITIES_REPO=git@github.com:your-username/clawd-capabilities.git

# Default branch for deployments
CLAWD_GIT_BRANCH=main

# ─────────────────────────────────────────────────────────────────────────────────
# API CREDENTIALS (Add as needed for your capabilities)
# ─────────────────────────────────────────────────────────────────────────────────
# Add credentials for services your capabilities integrate with.
# These are examples - add/remove based on your needs.

# X (Twitter) API
# X_API_KEY=your-api-key
# X_API_SECRET=your-api-secret
# X_ACCESS_TOKEN=your-access-token
# X_ACCESS_SECRET=your-access-secret

# Instantly.ai API
# INSTANTLY_API_KEY=your-instantly-api-key

# OpenAI (if used for embeddings or generation)
# OPENAI_API_KEY=your-openai-key

# Anthropic (if using Claude API directly)
# ANTHROPIC_API_KEY=your-anthropic-key

# ─────────────────────────────────────────────────────────────────────────────────
# ARCHON MCP CONFIGURATION (Optional)
# ─────────────────────────────────────────────────────────────────────────────────
# If using Archon for RAG and task management

# ARCHON_API_URL=http://localhost:8000
# ARCHON_API_KEY=your-archon-key

# ─────────────────────────────────────────────────────────────────────────────────
# ADVANCED OPTIONS
# ─────────────────────────────────────────────────────────────────────────────────

# SSH connection timeout in seconds
# CLAWD_SSH_TIMEOUT=10

# Number of backup copies to keep
# CLAWD_BACKUP_RETENTION=5

# Enable verbose logging during deployment
# CLAWD_VERBOSE=false
